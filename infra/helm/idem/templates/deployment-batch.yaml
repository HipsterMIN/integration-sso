# ============================================================
# Outbox Relay Batch — K8s Deployment
#
# 설계:
#  - ShedLock 분산 락 → 다중 Pod 중 단일 인스턴스만 Job 실행
#  - replicaCount=2 권장 (HA 페일오버) — 단일 인스턴스에서도 동작
#  - RollingUpdate maxUnavailable=0 → 무중단 배포
#  - terminationGracePeriodSeconds=45 → 진행 중 Job 완료 대기
#    (application.yml: spring.lifecycle.timeout-per-shutdown-phase=30s)
#  - 외부 노출 없음 → ClusterIP Service만 (Prometheus scrape용)
# ============================================================
{{- if .Values.relay.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idem-relay
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "onepass.labels" . | nindent 4 }}
    app.kubernetes.io/component: batch
  annotations:
    # Secret/ConfigMap 변경 시 Pod 재시작 트리거
    checksum/secret: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
    checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
spec:
  replicas: {{ .Values.relay.replicaCount }}
  selector:
    matchLabels:
      app: idem-relay
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: idem-relay
        {{- include "onepass.labels" . | nindent 8 }}
        app.kubernetes.io/component: batch
      {{- with .Values.relay.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      {{- if and .Values.relay.serviceAccount .Values.relay.serviceAccount.create }}
      serviceAccountName: {{ .Values.relay.serviceAccount.name | default "idem-relay" }}
      {{- end }}
      # 진행 중 Job 완료 대기 + Spring graceful shutdown 30s 여유분
      terminationGracePeriodSeconds: 45
      containers:
        - name: batch
          image: {{ include "onepass.image" .Values.relay.image }}
          imagePullPolicy: {{ .Values.relay.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8090
              protocol: TCP
          envFrom:
            - configMapRef:
                name: idem-relay-config
            - configMapRef:
                name: idem-common-config
            - secretRef:
                name: {{ .Values.relay.secretRef.name }}
          {{- if .Values.relay.startupProbe }}
          startupProbe:
            {{- toYaml .Values.relay.startupProbe | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.relay.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.relay.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.relay.resources | nindent 12 }}
      # Pod Anti-Affinity — ShedLock HA 시 서로 다른 노드 우선 배치
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: idem-relay
                topologyKey: kubernetes.io/hostname
{{- end }}
