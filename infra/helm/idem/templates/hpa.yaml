# ============================================================
# HorizontalPodAutoscaler — Q-Sign / Q-IM / IdO
#
# 정책:
#   - autoscaling/v2 (안정 API, K8s 1.23+)
#   - CPU + Memory 듀얼 메트릭 (모두 임계 초과 시 스케일아웃)
#   - scaleDown stabilizationWindow: 5분 — Handoff 스파이크 후 즉시 축소 방지
#   - scaleUp stabilizationWindow: 30초 — 부하 증가 시 빠른 확장
#
# ⚠️ outbox-relay-batch는 의도적으로 HPA 미적용:
#    ShedLock이 단일 인스턴스만 Job을 실행하므로 Pod 증가가 처리량을 늘리지 못함.
#    가용성은 PDB(minAvailable=1) + replicaCount=2 고정으로 보장.
# ============================================================

# ── Q-Sign HPA ────────────────────────────────────────────────
{{- if and .Values.gate.enabled .Values.gate.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-idem-gate
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "onepass.labels" . | nindent 4 }}
    app.kubernetes.io/component: idem-gate
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: idem-gate
  minReplicas: {{ .Values.gate.hpa.minReplicas }}
  maxReplicas: {{ .Values.gate.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.gate.hpa.targetCPUUtilizationPercentage }}
    {{- if .Values.gate.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.gate.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
        - type: Pods
          value: 2
          periodSeconds: 30
      selectPolicy: Max
{{- end }}
---
# ── Q-IM HPA ──────────────────────────────────────────────────
{{- if and .Values.registry.enabled .Values.registry.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-idem-registry
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "onepass.labels" . | nindent 4 }}
    app.kubernetes.io/component: idem-registry
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: idem-registry
  minReplicas: {{ .Values.registry.hpa.minReplicas }}
  maxReplicas: {{ .Values.registry.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.registry.hpa.targetCPUUtilizationPercentage }}
    {{- if .Values.registry.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.registry.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
{{- end }}
---
# ── IdO HPA ───────────────────────────────────────────────────
{{- if and .Values.hub.enabled .Values.hub.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-idem-hub
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "onepass.labels" . | nindent 4 }}
    app.kubernetes.io/component: idem-hub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: idem-hub
  minReplicas: {{ .Values.hub.hpa.minReplicas }}
  maxReplicas: {{ .Values.hub.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hub.hpa.targetCPUUtilizationPercentage }}
    {{- if .Values.hub.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.hub.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleDown:
      # ido는 SLO 영향이 가장 큼 → 더 보수적 (10분)
      stabilizationWindowSeconds: 600
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
    scaleUp:
      # 부하 급증 시 즉각 대응
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
{{- end }}
---
# ── Q-AuthZ HPA ───────────────────────────────────────────────
{{- if and .Values.authz.enabled .Values.authz.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-authz
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "onepass.labels" . | nindent 4 }}
    app.kubernetes.io/component: idem-authz
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: idem-authz
  minReplicas: {{ .Values.authz.hpa.minReplicas }}
  maxReplicas: {{ .Values.authz.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.authz.hpa.targetCPUUtilizationPercentage }}
    {{- if .Values.authz.hpa.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.authz.hpa.targetMemoryUtilizationPercentage }}
    {{- end }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 50
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
{{- end }}
