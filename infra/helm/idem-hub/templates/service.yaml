apiVersion: v1
kind: Service
metadata:
  name: idem-hub-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "idem.hub.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "idem.hub.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: {{ .Values.service.type }}
