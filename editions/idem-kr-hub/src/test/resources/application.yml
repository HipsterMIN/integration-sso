# 테스트 기본 설정은 idem-hub 테스트 픽스처(src/testFixtures/resources/application-hub-test-base.yml)를 가져온다 (S8-a).
# 이 파일이 테스트 클래스패스 맨 앞에 있어 hub main 의 application.yml 을 가린다 — KR 에디션 테스트도 같은 방식.
spring:
  config:
    import: classpath:application-hub-test-base.yml
